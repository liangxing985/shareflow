# ShareFlow 支付对接接口文档

**版本**：v1.3.1
**更新日期**：2026-09-15
**适用系统**：ShareFlow 个人收款分账系统

---

## 目录

1. [配置信息](#1-配置信息)
2. [签名算法](#2-签名算法)
3. [创建支付订单](#3-创建支付订单)
4. [查询支付状态](#4-查询支付状态)
5. [支付回调通知](#5-支付回调通知)
6. [错误码说明](#6-错误码说明)
7. [对接示例](#7-对接示例)
8. [常见问题](#8-常见问题)

---

## 1. 配置信息

| 配置项 | 说明 | 示例值 |
|---|---|---|
| **API 根地址** | ShareFlow 接口基础地址 | `http://118.25.48.22:8080/api/v1` |
| **商户 ID / AppID** | 接入方标识，在 ShareFlow 后台配置 | `peiwan_app_001` |
| **通信密钥** | API Key，用于签名验证，32位随机字符串 | `sk_a1b2c3d4e5f6...` |
| **回调地址** | 支付成功后 ShareFlow 主动 POST 通知的地址 | `https://your-domain.com/api/payment/notify` |
| **对接方式** | 服务器端调用 + 前端跳转/轮询 | Server-to-Server |

### 1.1 服务器端配置

在 ShareFlow 服务器 `.env` 文件中配置：

```bash
# 商户ID/AppID
PAYMENT_APP_ID=peiwan_app_001

# 默认回调地址（创建订单时未传notify_url时使用此地址）
PAYMENT_NOTIFY_URL=https://your-domain.com/api/payment/notify

# 回调最大重试次数
PAYMENT_NOTIFY_RETRY_MAX=3

# 是否启用签名验证（生产环境必须开启）
PAYMENT_SIGN_ENABLED=true

# API Key（通信密钥，多个用逗号分隔）
API_KEYS=sk_你的32位随机密钥
```

### 1.2 生成通信密钥

```bash
openssl rand -hex 32
```

---

## 2. 签名算法

### 2.1 签名方式

- **算法**：MD5
- **输出**：32位小写十六进制字符串

### 2.2 签名步骤

1. **过滤参数**：移除 `sign` 参数和空值参数（值为 `null`、`""` 的参数不参与签名）
2. **参数排序**：按参数名 ASCII 码从小到大排序
3. **拼接字符串**：拼接成 `key1=value1&key2=value2...` 格式
4. **拼接密钥**：在末尾拼接 `&key=通信密钥`
5. **MD5加密**：对整个字符串进行 MD5 加密，得到32位小写字符串

### 2.3 签名示例

**请求参数**：
```json
{
  "app_id": "peiwan_app_001",
  "out_order_no": "PW202609150001",
  "total_amount": "99.00",
  "product_name": "王者荣耀陪玩2小时",
  "payer_name": "客户张三",
  "category": "peiwan",
  "timestamp": "1757932800"
}
```

**排序后拼接**：
```
app_id=peiwan_app_001&category=peiwan&out_order_no=PW202609150001&payer_name=客户张三&product_name=王者荣耀陪玩2小时&timestamp=1757932800&total_amount=99.00&key=sk_你的32位随机密钥
```

**MD5签名**：
```
sign = md5("app_id=peiwan_app_001&category=peiwan&out_order_no=PW202609150001&payer_name=客户张三&product_name=王者荣耀陪玩2小时&timestamp=1757932800&total_amount=99.00&key=sk_你的32位随机密钥")
```

### 2.4 注意事项

- 金额必须保留2位小数，如 `99.00`（不能是 `99.0` 或 `99`）
- 空值参数不参与签名
- 签名必须是32位小写
- 时间戳为10位秒级时间戳

---

## 3. 创建支付订单

### 3.1 接口信息

| 项目 | 说明 |
|---|---|
| **接口地址** | `POST {API根地址}/payment/create` |
| **请求方式** | POST |
| **Content-Type** | `application/json` |
| **是否需要登录** | 否（通过签名验证） |

### 3.2 请求参数

| 参数名 | 类型 | 必填 | 长度限制 | 说明 |
|---|---|---|---|---|
| app_id | string | 是 | 64 | 商户ID/AppID，与ShareFlow配置一致 |
| out_order_no | string | 是 | 64 | 商户订单号（你的业务系统订单号，必须唯一） |
| total_amount | string | 是 | - | 支付金额（元），保留2位小数，如 `99.00` |
| product_name | string | 是 | 200 | 商品/业务名称 |
| product_desc | string | 否 | 1000 | 商品描述 |
| payer_name | string | 否 | 100 | 付款人名称/昵称 |
| payer_contact | string | 否 | 100 | 付款人联系方式 |
| category | string | 否 | 50 | 业务分类（用于匹配分账规则，如 `peiwan`） |
| remark | string | 否 | 500 | 备注 |
| timestamp | string | 是 | 10 | 当前时间戳（秒级），5分钟内有效 |
| sign | string | 是 | 32 | MD5签名（见签名算法） |

### 3.3 请求示例

```bash
curl -X POST "http://118.25.48.22:8080/api/v1/payment/create" \
  -H "Content-Type: application/json" \
  -d '{
    "app_id": "peiwan_app_001",
    "out_order_no": "PW202609150001",
    "total_amount": "99.00",
    "product_name": "王者荣耀陪玩2小时",
    "product_desc": "钻石段位，技术陪玩",
    "payer_name": "客户张三",
    "payer_contact": "13800138000",
    "category": "peiwan",
    "remark": "陪玩订单",
    "timestamp": "1757932800",
    "sign": "e10adc3949ba59abbe56e057f20f883e"
  }'
```

### 3.4 返回格式

**成功响应**：
```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "order_no": "SF20260915123456ABCDEF",
    "out_order_no": "PW202609150001",
    "total_amount": "99.00",
    "pay_url": "/pay/SF20260915123456ABCDEF",
    "qr_code_urls": {
      "wechat": "http://118.25.48.22:8080/uploads/20260915xxxx_wechat.jpg",
      "alipay": "http://118.25.48.22:8080/uploads/20260915xxxx_alipay.jpg"
    },
    "expire_at": "2026-09-15 12:40:00",
    "pay_status": "pending_pay"
  }
}
```

**返回字段说明**：

| 字段名 | 类型 | 说明 |
|---|---|---|
| code | int | 状态码，0=成功，非0=失败 |
| msg | string | 提示信息 |
| data.order_no | string | ShareFlow系统订单号 |
| data.out_order_no | string | 商户订单号 |
| data.total_amount | string | 支付金额（保留2位小数） |
| data.pay_url | string | 支付页面相对路径，完整URL = API根地址去掉/api/v1 + pay_url |
| data.qr_code_urls.wechat | string | 微信收款码图片URL（完整地址） |
| data.qr_code_urls.alipay | string | 支付宝收款码图片URL（完整地址） |
| data.expire_at | string | 支付链接过期时间（5分钟） |
| data.pay_status | string | 支付状态：pending_pay（待支付） |

**支付链接完整地址计算**：
```
完整支付地址 = "http://118.25.48.22:8080" + data.pay_url
例如：http://118.25.48.22:8080/pay/SF20260915123456ABCDEF
```

**失败响应**：
```json
{
  "code": -1,
  "msg": "签名验证失败",
  "data": null
}
```

---

## 4. 查询支付状态

### 4.1 接口信息

| 项目 | 说明 |
|---|---|
| **接口地址** | `GET {API根地址}/payment/status/{order_no}` |
| **请求方式** | GET |
| **是否需要登录** | 否（公开接口，前端轮询用） |

### 4.2 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|---|---|---|---|
| order_no | string | 是 | ShareFlow系统订单号（URL路径参数） |

### 4.3 请求示例

```bash
curl "http://118.25.48.22:8080/api/v1/payment/status/SF20260915123456ABCDEF"
```

### 4.4 返回格式

**成功响应**：
```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "order_no": "SF20260915123456ABCDEF",
    "out_order_no": "PW202609150001",
    "total_amount": "99.00",
    "pay_status": "paid",
    "paid_at": "2026-09-15 12:35:30",
    "expire_at": "2026-09-15 12:40:00"
  }
}
```

**支付状态说明**：

| 状态 | 说明 |
|---|---|
| pending_pay | 待支付（客户还未扫码或未点"我已支付"） |
| pending_confirm | 待确认（客户已点"我已支付"，等待管理员确认收款） |
| paid | 已支付（管理员已确认收款，分账完成） |
| expired | 已过期（超过5分钟未支付） |

### 4.5 前端轮询建议

- 轮询间隔：3秒
- 轮询超时：5分钟（与支付链接有效期一致）
- 轮询到 `paid` 状态后停止轮询，提示支付成功
- 轮询到 `expired` 状态后停止轮询，提示支付过期，引导重新下单

---

## 5. 支付回调通知

### 5.1 接口信息

| 项目 | 说明 |
|---|---|
| **回调方式** | ShareFlow 服务器主动 POST 通知到你的回调地址 |
| **请求方式** | POST |
| **Content-Type** | `application/json` |
| **触发时机** | 管理员在 ShareFlow 后台确认收款后 |
| **重试机制** | 失败自动重试，最多3次，间隔1分钟、5分钟、10分钟 |

### 5.2 回调参数

| 参数名 | 类型 | 说明 |
|---|---|---|
| app_id | string | 商户ID/AppID |
| order_no | string | ShareFlow系统订单号 |
| out_order_no | string | 商户订单号 |
| total_amount | string | 支付金额（保留2位小数） |
| pay_status | string | 支付状态：paid（已支付） |
| paid_at | string | 支付时间（格式：YYYY-MM-DD HH:MM:SS） |
| transaction_id | string | 微信/支付宝交易号（如有，可能为空） |
| timestamp | string | 回调时间戳（秒级） |
| sign | string | MD5签名（见签名算法） |

### 5.3 回调示例

```json
{
  "app_id": "peiwan_app_001",
  "order_no": "SF20260915123456ABCDEF",
  "out_order_no": "PW202609150001",
  "total_amount": "99.00",
  "pay_status": "paid",
  "paid_at": "2026-09-15 12:35:30",
  "transaction_id": "4200001234567890",
  "timestamp": "1757933130",
  "sign": "e10adc3949ba59abbe56e057f20f883e"
}
```

### 5.4 回调处理步骤

1. **验证签名**：使用相同的签名算法计算签名，与传入的 `sign` 比对
2. **验证支付状态**：确认 `pay_status` 为 `paid`
3. **验证金额**：确认 `total_amount` 与你的订单金额一致
4. **处理订单**：更新你的业务系统订单状态、发货、通知陪玩师等
5. **返回成功**：返回 `{"code": 0, "msg": "success"}`

### 5.5 回调返回要求

**接收成功（必须返回）**：
```json
{
  "code": 0,
  "msg": "success"
}
```

**接收失败（返回非0 code，ShareFlow会重试）**：
```json
{
  "code": -1,
  "msg": "签名验证失败"
}
```

### 5.6 注意事项

- 回调地址必须是公网可访问的URL
- 必须返回 `code: 0` 表示接收成功，否则ShareFlow会重试
- 回调可能重复发送（重试机制），你的系统需要做好**幂等处理**（同一订单多次回调不会重复处理）
- 回调是后台异步发送，不依赖客户操作
- 管理员确认收款后才会触发回调，客户点"我已支付"不会触发回调

---

## 6. 错误码说明

| 错误码 | 说明 | 处理建议 |
|---|---|---|
| 0 | 成功 | - |
| -1 | 通用失败 | 查看 msg 字段的具体错误信息 |
| -1 | 缺少必填参数: xxx | 检查请求参数是否完整 |
| -1 | 时间戳已过期 | 检查服务器时间是否准确，时间戳必须在5分钟内 |
| -1 | 签名验证失败 | 检查签名算法、API Key、参数排序、金额格式 |
| -1 | app_id不匹配 | 检查 app_id 是否与ShareFlow配置一致 |
| -1 | 金额格式错误 | 金额必须是数字，保留2位小数 |
| -1 | 金额必须大于0 | 检查金额是否为正数 |
| -1 | 金额超过上限 | 单笔最大100万元 |
| -1 | 外部订单号已存在 | out_order_no 必须唯一，检查是否重复下单 |
| -1 | 订单不存在 | 检查 order_no 是否正确 |
| -1 | 订单号格式错误 | order_no 只能是字母和数字，最长64位 |
| -1 | 服务器未配置API Key | 联系管理员在 .env 中配置 API_KEYS |
| 400 | 请求体格式错误 | 检查请求是否为合法JSON |
| 500 | 服务器内部错误 | 联系管理员查看日志 |

---

## 7. 对接示例

### 7.1 Python 完整示例

```python
import hashlib
import time
import requests
from flask import Flask, request, jsonify

app = Flask(__name__)

# ===== 配置 =====
SHAREFLOW_API = "http://118.25.48.22:8080/api/v1"
SHAREFLOW_BASE = "http://118.25.48.22:8080"
APP_ID = "peiwan_app_001"
API_KEY = "sk_你的32位随机密钥"


def generate_sign(params):
    """生成MD5签名"""
    filtered = {k: v for k, v in params.items() if k != 'sign' and v}
    sorted_keys = sorted(filtered.keys())
    sign_str = '&'.join([f'{k}={filtered[k]}' for k in sorted_keys])
    sign_str += f'&key={API_KEY}'
    return hashlib.md5(sign_str.encode()).hexdigest()


def verify_sign(params):
    """验证签名"""
    sign = params.pop('sign', '')
    expected_sign = generate_sign(params)
    return sign == expected_sign


def create_payment_order(out_order_no, amount, product_name, payer_name=""):
    """创建支付订单"""
    params = {
        "app_id": APP_ID,
        "out_order_no": out_order_no,
        "total_amount": f"{amount:.2f}",
        "product_name": product_name,
        "payer_name": payer_name,
        "category": "peiwan",
        "timestamp": str(int(time.time()))
    }
    params["sign"] = generate_sign(params)
    
    response = requests.post(f"{SHAREFLOW_API}/payment/create", json=params, timeout=10)
    return response.json()


def query_payment_status(order_no):
    """查询支付状态"""
    response = requests.get(f"{SHAREFLOW_API}/payment/status/{order_no}", timeout=10)
    return response.json()


# ===== 你的业务接口 =====

@app.route('/api/peiwan/create-order', methods=['POST'])
def peiwan_create_order():
    """陪玩系统下单接口"""
    data = request.json
    
    # 调用ShareFlow创建支付订单
    result = create_payment_order(
        out_order_no=data["order_no"],
        amount=float(data["amount"]),
        product_name=data["service_name"],
        payer_name=data.get("customer_name", "")
    )
    
    if result["code"] == 0:
        # 返回完整支付地址给前端
        result["data"]["pay_full_url"] = SHAREFLOW_BASE + result["data"]["pay_url"]
    
    return jsonify(result)


@app.route('/api/payment/notify', methods=['POST'])
def payment_notify():
    """接收ShareFlow支付回调"""
    params = request.json
    
    # 1. 验证签名
    if not verify_sign(params.copy()):
        return jsonify({"code": -1, "msg": "签名验证失败"})
    
    # 2. 验证支付状态
    if params["pay_status"] != "paid":
        return jsonify({"code": -1, "msg": "支付状态异常"})
    
    # 3. 处理订单（幂等处理：同一订单多次回调只处理一次）
    out_order_no = params["out_order_no"]
    total_amount = params["total_amount"]
    paid_at = params["paid_at"]
    
    # TODO: 更新你的数据库订单状态
    # 注意：需要判断订单是否已处理，避免重复处理
    # if order.status != 'paid':
    #     update order status to paid
    #     notify 陪玩师
    
    print(f"订单 {out_order_no} 支付成功，金额 {total_amount}，时间 {paid_at}")
    
    # 4. 返回成功
    return jsonify({"code": 0, "msg": "success"})


if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)
```

### 7.2 Node.js 完整示例

```javascript
const express = require('express')
const crypto = require('crypto')
const axios = require('axios')

const app = express()
app.use(express.json())

// ===== 配置 =====
const SHAREFLOW_API = 'http://118.25.48.22:8080/api/v1'
const SHAREFLOW_BASE = 'http://118.25.48.22:8080'
const APP_ID = 'peiwan_app_001'
const API_KEY = 'sk_你的32位随机密钥'

function generateSign(params) {
  const filtered = Object.fromEntries(
    Object.entries(params).filter(([k, v]) => k !== 'sign' && v)
  )
  const sortedKeys = Object.keys(filtered).sort()
  const signStr = sortedKeys.map(k => `${k}=${filtered[k]}`).join('&') + `&key=${API_KEY}`
  return crypto.createHash('md5').update(signStr).digest('hex')
}

function verifySign(params) {
  const sign = params.sign
  delete params.sign
  return sign === generateSign(params)
}

async function createPaymentOrder(outOrderNo, amount, productName, payerName = '') {
  const params = {
    app_id: APP_ID,
    out_order_no: outOrderNo,
    total_amount: amount.toFixed(2),
    product_name: productName,
    payer_name: payerName,
    category: 'peiwan',
    timestamp: Math.floor(Date.now() / 1000).toString()
  }
  params.sign = generateSign(params)
  
  const response = await axios.post(`${SHAREFLOW_API}/payment/create`, params)
  return response.data
}

// ===== 你的业务接口 =====

app.post('/api/peiwan/create-order', async (req, res) => {
  try {
    const result = await createPaymentOrder(
      req.body.order_no,
      parseFloat(req.body.amount),
      req.body.service_name,
      req.body.customer_name
    )
    
    if (result.code === 0) {
      result.data.pay_full_url = SHAREFLOW_BASE + result.data.pay_url
    }
    
    res.json(result)
  } catch (error) {
    res.json({ code: -1, msg: error.message })
  }
})

app.post('/api/payment/notify', (req, res) => {
  const params = req.body
  
  // 1. 验证签名
  if (!verifySign({ ...params })) {
    return res.json({ code: -1, msg: '签名验证失败' })
  }
  
  // 2. 验证支付状态
  if (params.pay_status !== 'paid') {
    return res.json({ code: -1, msg: '支付状态异常' })
  }
  
  // 3. 处理订单（幂等处理）
  const { out_order_no, total_amount, paid_at } = params
  console.log(`订单 ${out_order_no} 支付成功，金额 ${total_amount}，时间 ${paid_at}`)
  // TODO: 更新你的数据库订单状态
  
  // 4. 返回成功
  res.json({ code: 0, msg: 'success' })
})

app.listen(5000, () => {
  console.log('Server running on port 5000')
})
```

### 7.3 前端跳转支付示例

```javascript
// 方式1：跳转到ShareFlow支付页（最简单）
async function goPay() {
  const res = await fetch('/api/peiwan/create-order', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      order_no: 'PW' + Date.now(),
      amount: 99.00,
      service_name: '王者荣耀陪玩2小时',
      customer_name: '客户张三'
    })
  })
  const data = await res.json()
  
  if (data.code === 0) {
    // 跳转到ShareFlow支付页
    window.location.href = data.data.pay_full_url
  } else {
    alert('创建订单失败: ' + data.msg)
  }
}

// 方式2：自己展示二维码 + 轮询状态（体验更好）
async function showQrCode() {
  const res = await fetch('/api/peiwan/create-order', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      order_no: 'PW' + Date.now(),
      amount: 99.00,
      service_name: '王者荣耀陪玩2小时'
    })
  })
  const data = await res.json()
  
  if (data.code === 0) {
    // 展示二维码
    document.getElementById('wechat-qr').src = data.data.qr_code_urls.wechat
    document.getElementById('alipay-qr').src = data.data.qr_code_urls.alipay
    
    // 轮询支付状态
    const orderNo = data.data.order_no
    const timer = setInterval(async () => {
      const statusRes = await fetch(
        `http://118.25.48.22:8080/api/v1/payment/status/${orderNo}`
      )
      const statusData = await statusRes.json()
      
      if (statusData.data.pay_status === 'paid') {
        clearInterval(timer)
        alert('支付成功！')
        // 跳转到成功页或刷新订单状态
      } else if (statusData.data.pay_status === 'expired') {
        clearInterval(timer)
        alert('支付已过期，请重新下单')
      }
    }, 3000)
  }
}
```

---

## 8. 常见问题

### Q1：回调地址必须是公网可访问的吗？
A：是的，ShareFlow 服务器需要能访问到你的回调地址。本地开发可以用内网穿透工具（如 ngrok、frp）。

### Q2：回调失败会重试吗？
A：会，最多重试3次，间隔1分钟、5分钟、10分钟。你的系统需要做好幂等处理（同一订单多次回调不会重复处理）。

### Q3：签名验证失败怎么办？
A：检查以下几点：
1. API Key 是否和 ShareFlow 配置一致
2. 金额是否保留2位小数（如 `99.00` 而不是 `99.0` 或 `99`）
3. 空值参数是否参与签名（空值不参与签名）
4. 参数是否按 ASCII 排序
5. 签名是否为32位小写

### Q4：支付链接有效期多久？
A：5分钟。过期后订单状态变为 `expired`，需要重新下单。

### Q5：可以不跳转，直接在业务系统内完成支付吗？
A：可以，使用方式2（自己展示二维码 + 轮询状态），客户不需要离开业务系统。

### Q6：客户点"我已支付"后会立即触发回调吗？
A：不会。客户点"我已支付"后订单状态变为 `pending_confirm`（待确认），需要管理员在 ShareFlow 后台确认收款后才会触发回调。

### Q7：如何确认收款？
A：管理员登录 ShareFlow 后台 → 订单管理 → 找到"待确认"状态的订单 → 核对微信/支付宝到账情况 → 点击"确认收款"按钮。

### Q8：单笔金额有限制吗？
A：单笔最大100万元。

### Q9：out_order_no 可以重复吗？
A：不可以，必须唯一。重复的 out_order_no 会返回"外部订单号已存在"错误。

### Q10：时间戳验证失败怎么办？
A：检查你的服务器时间是否准确，时间戳必须是当前时间的10位秒级时间戳，且在5分钟内有效。建议使用 NTP 同步服务器时间。

---

## 附录：完整对接流程

```
┌─────────────┐     1.下单     ┌──────────────────┐
│  客户/前端   │ ──────────────> │  你的业务系统后端  │
└─────────────┘                  └────────┬─────────┘
                                            │
                              2.调用 /payment/create（带签名）
                                            │
                                            ▼
                                  ┌──────────────────┐
                                  │   ShareFlow 服务器  │
                                  │  创建订单，生成二维码 │
                                  └────────┬─────────┘
                                            │
                              3.返回 pay_url + qr_code_urls
                                            │
┌─────────────┐     4.跳转/展示二维码     │
│  客户/前端   │ <─────────────────────────┘
└──────┬──────┘
       │
       │ 5.扫码支付
       ▼
┌─────────────┐
│ 微信/支付宝  │
└──────┬──────┘
       │
       │ 6.客户点"我已支付"
       ▼
┌──────────────────┐
│  ShareFlow 服务器  │ 订单状态：pending_confirm
└────────┬─────────┘
         │
         │ 7.管理员确认收款
         ▼
┌──────────────────┐
│  ShareFlow 服务器  │ 订单状态：paid，自动分账
└────────┬─────────┘
         │
         │ 8.POST 回调通知（带签名）
         ▼
┌──────────────────┐
│  你的业务系统后端  │ 验证签名 → 更新订单 → 返回 code=0
└────────┬─────────┘
         │
         │ 9.通知陪玩师/发货
         ▼
┌─────────────┐
│  客户/前端   │ 支付成功，订单完成
└─────────────┘
```

---

**文档结束**

如有疑问，请联系 ShareFlow 系统管理员。
